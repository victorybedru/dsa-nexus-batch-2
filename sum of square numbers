class Solution(object):
    def judgeSquareSum(self, c):
        a=0
        while a**2<=c:
            b= c- a**2
            sqr= int(math.sqrt(b))
            if sqr**2==b:
                return True
            a+=1
        return False
